version: '3'
services:
    training-swoole-api:
        build:
            context: ./amd64
            dockerfile: Dockerfile
        tty: yes
        ports:
            - '80:80'
        volumes:
            - ../api/dispachMode1/:/var/www
        restart: always
